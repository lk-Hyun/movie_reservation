spring:
  datasource:
    url: jdbc:mysql://localhost:3306/movie_reservation
    username: root
    password: skq1w2e#
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        show_sql: true
        format_sql: true

